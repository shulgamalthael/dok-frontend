const config = {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {},
        'postcss-nested': {},
        cssnano: {
            preset: 'default',
        },
    },
};

export default config;
